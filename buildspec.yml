version: 0.2

phases:
  build:
    commands:
      - echo "Preparing Lambda deployment packages..."

artifacts:
  files:
    - '**/*'

  secondary-artifacts:
    BuildGetProducts:
      base-directory: backend/get-products
      files:
        - '**/*'

    BuildCreateOrder:
      base-directory: backend/create-order
      files:
        - '**/*'

    BuildOrderProcessor:
      base-directory: backend/order-processor
      files:
        - '**/*'
